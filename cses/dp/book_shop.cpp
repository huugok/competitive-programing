#include <bits/stdc++.h>
using namespace std;


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int numBooks;
    int maxPrice;
    cin >> numBooks;
    cin >> maxPrice;

    vector<int> books(price);
    for( int i{}; i < numBooks; i++) cin >> books[maxPrice];

    vector<int> pages(numBooks);
    for(int i{};i < numBooks; i++)

}